# Adversarial config - attempts to bypass detection

[project]
id = "adversarial-test"
name = "Adversarial Test Project"

[auth]
# BYPASS ATTEMPT: Using different casing
Enable_Confirmations = true
# But the actual setting is lowercase and false
enable_confirmations = false

# BYPASS ATTEMPT: OTP set via environment variable reference
# Scanner might not catch this
otp_exp = "${OTP_EXPIRY:-7200}"

[auth.smtp]
# BYPASS ATTEMPT: Empty SMTP config still counts as "configured"?
host = ""
